[project]
name = "family-cookbook"
version = "0.1.0"
description = "Extract recipes from photos of cookbooks, magazines, and handwritten cards into a searchable static website"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=2.24.0",
    "pillow>=12.1.1",
    "python-dotenv>=1.2.2",
    "python-frontmatter>=1.1.0",
    "pyyaml>=6.0.3",
    "zensical>=0.0.24",
]

[tool.uv]
package = false
